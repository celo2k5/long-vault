import React from 'react';import {createRoot} from 'react-dom/client';import VaultDashboard from '../app/VaultDashboard';import '../app/globals.css';createRoot(document.getElementById('root')!).render(<VaultDashboard view={location.pathname==='/admin'?'admin':'overview'}/>);
